// Babel config for the library build (react-native-builder-bob).
// The lib uses only relative imports, so no module-resolver is needed here.
module.exports = {
  presets: ['module:@react-native/babel-preset'],
};
